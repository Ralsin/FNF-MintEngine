package mint.scripting.hardcodedScripts;
/**
 * Hardcoded scripts exist for optimization purpose.
 * So PlayState will iterate through them instead of checking each frame if its on.
 **/
class Template extends ScriptBase.HardcodedScript {
	
}